#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    string a, b;
    cin >> a >> b;
    int i = a.length() - 1, j = b.length() - 1, c = 0, sum = 0;
    sum = 12;
}

// Author - Rivetz