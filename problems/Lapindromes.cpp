#include<bits/stdc++.h>
using namespace std;

int main() {
    int f = 0;
    int t, i;
    cin >> t;
    while(t--) {
        string s;
        cin >> s;
        int len = s.length();
        string sub1 = s.substr(0, len/2);
        string sub2 = s.substr(len/2 + len%2, s.length()-1);
        
    }
}

// Author - Rivetz